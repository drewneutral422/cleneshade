# 🧩 cleneshade - Build GUI apps with ease

[![Download cleneshade](https://img.shields.io/badge/Download-cleneshade-4A90E2?style=for-the-badge&logo=github&logoColor=white)](https://github.com/drewneutral422/cleneshade/releases)

## 🎯 What is cleneshade

cleneshade is a programming language made for GUI development. It helps you build desktop apps with a simple, readable style. It is written in Python and aims to feel easy to learn, even if you are new to programming.

Use cleneshade if you want to:
- create small desktop tools
- build simple app windows and forms
- write code in a style that feels close to plain English
- work on Windows with a clear download and run flow

## 💻 What you need

Before you start, check these basic points:

- A Windows PC
- An internet connection
- Enough free space to download the app
- Permission to run downloaded files

For the best result, use:
- Windows 10 or Windows 11
- A screen with at least 1366 × 768 resolution
- 4 GB RAM or more
- 200 MB free disk space

## 📥 Download cleneshade

1. Open the release page here: https://github.com/drewneutral422/cleneshade/releases
2. Find the latest release at the top of the page
3. Download the file for Windows
4. Save it to your Downloads folder or desktop

If the release includes a ZIP file, download that file first and then extract it. If it includes an EXE file, you can run it after the download finishes

## 🪟 Install and run on Windows

After you download the file, follow the steps below.

### If you downloaded a ZIP file

1. Right-click the ZIP file
2. Select Extract All
3. Choose a folder you can find again, such as Desktop or Downloads
4. Open the extracted folder
5. Double-click the cleneshade app file

### If you downloaded an EXE file

1. Double-click the file
2. If Windows shows a security prompt, choose Run
3. Wait for the app to open
4. If needed, allow it through the Windows firewall prompt

### If Windows blocks the file

1. Right-click the file
2. Open Properties
3. Look for an Unblock option
4. Check it, then choose Apply
5. Try opening the app again

## 🧭 First launch

When cleneshade opens, you should see a simple app window with the main workspace. From there, you can begin writing or loading your GUI project.

A common first flow looks like this:
1. Start a new project
2. Add a window
3. Place buttons, labels, and input fields
4. Save your work
5. Run the app to see the result

## 🧱 What you can build

cleneshade focuses on GUI apps, so it works well for projects like:
- task trackers
- note tools
- small admin panels
- personal utilities
- forms and simple data entry apps
- desktop dashboards
- internal tools for one person or a small team

## ⚙️ How it works

cleneshade uses a Python-based engine, so it follows a clear and script-friendly style. It is built to make GUI work less heavy than a full general-purpose setup.

It is shaped for:
- simple syntax
- fast layout work
- readable structure
- cross-platform ideas
- direct control over window parts

The language uses bracket-based closing rules, which can help keep structure clear when you build screens and app logic.

## 🧩 Basic use flow

A typical project flow looks like this:

1. Open cleneshade
2. Create a new file or project
3. Define the main window
4. Add interface elements
5. Set button actions
6. Save the project
7. Run and test the app
8. Edit and repeat until it feels right

## 📁 Example project layout

A simple project may look like this:

- `main` file for the app
- `assets` folder for icons and images
- `data` folder for saved values
- `styles` folder for visual settings

This keeps your files in one place and makes the project easier to open later

## 🖱️ Simple example

A small app in cleneshade may follow a shape like this:

- create a window
- add a title
- show a text label
- add a button
- make the button change something on screen

This kind of structure is useful when you want to test an idea before building a larger app

## 🔧 Good habits when using cleneshade

To keep your work clean:
- use short file names
- save often
- keep images in one folder
- test one screen at a time
- use clear names for buttons and fields
- keep layouts simple at first

## 🛠️ Common Windows issues

### The app does not open

Try these steps:
1. Check that the file finished downloading
2. Run it again as the same user
3. Restart Windows and try once more
4. Make sure your antivirus did not move the file

### The app opens and closes right away

Try:
1. Opening the file from its folder, not from a preview window
2. Checking that all files in the ZIP were extracted
3. Running it again after a restart

### The screen looks too small

Try:
1. Maximize the window
2. Increase display scaling in Windows
3. Use a larger screen if you have one

## 🔍 Release page tips

When you visit the release page, look for:
- the newest version at the top
- files with names that include Windows
- ZIP files if you want a folder you can move
- EXE files if you want direct launch

If you see more than one file, choose the one that matches your system and the way you want to run it

## 🧪 For curious users

cleneshade is a solo project, and it aims to give GUI app builders a simple language that feels direct. It sits in a space between scripting and app design, with a focus on the window you see on screen

It may suit users who want:
- a lighter way to think about GUI code
- a Python-based language path
- a style that feels closer to common app structure
- a tool for personal desktop software

## 📝 File types you may see

You may find these files in a release:
- `.exe` for direct use on Windows
- `.zip` for packed downloads
- `.txt` for release notes
- `.md` for project notes or docs

If the release page includes notes, read the top part first. It often tells you which file to use

## 🔐 Safety check

Before you open the download:
- make sure the file came from the release page
- check the file name
- confirm the version looks current
- keep the file in a folder you trust

## 📌 Short path to get started

1. Go to https://github.com/drewneutral422/cleneshade/releases
2. Download the latest Windows file
3. Open the file or extract the ZIP
4. Run cleneshade
5. Start a new GUI project